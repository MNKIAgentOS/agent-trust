# mnki-cli

demo, init, identity, verify, inspect, delegate, attest, revoke, scan, protect, conformance (`agenttrust` is an alias binary). `npx mnki-cli demo`. See https://github.com/MNKIAgentOS/agent-trust.
